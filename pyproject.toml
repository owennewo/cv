[project]
name = "cvgen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.6",
]

[tool.setuptools.packages.find]
where = ["cvgen"]
