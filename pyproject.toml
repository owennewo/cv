[project]
name = "cvgen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.6",
    "playwright>=1.48.0",
    "tool>=0.8.0",
]

[tool.setuptools.packages.find]
where = ["cvgen"]
